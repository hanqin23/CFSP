// [1] collect the four sentences from the user
let firstSentence = prompt("What is the first line of the event score?");
let secondSentence = prompt("What is the second line of the event score?");

// [2] create an empty array
let scoreList = [];

// [3] define the generator function
const generateScore = (first, second) => {
  // [4] push the user's input into an empty array
  // [5] in the generateScore function, iterate over that array
}

// [6] invoke the function passing in the user input
generateScore(firstSentence, secondSentence);